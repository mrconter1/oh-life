<!-- This is a placeholder. In a real project, you would convert the SVG to an actual ICO file.
     Since we can't directly create binary ICO files here, you should convert the SVG to ICO format
     using a tool like https://convertico.com or similar online converter. --> 